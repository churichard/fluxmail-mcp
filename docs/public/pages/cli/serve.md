---
title: 'fluxmail serve'
description: 'Run the HTTP server (MCP at /mcp and REST at /api/v1)'
updated: '2026-07-15'
---

<!-- This page is generated from the CLI command definitions. Run pnpm docs:generate to update it. -->

`fluxmail serve`

Run the HTTP server (MCP at /mcp and REST at /api/v1)

## Usage

```bash
fluxmail serve
```

## Options

This command has no command-specific options.
