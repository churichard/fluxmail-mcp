---
title: 'fluxmail config set'
description: 'Store a setting (shell env vars and local .env files still take precedence)'
updated: '2026-07-15'
---

<!-- This page is generated from the CLI command definitions. Run pnpm docs:generate to update it. -->

`fluxmail config set`

Store a setting (shell env vars and local .env files still take precedence)

## Usage

```bash
fluxmail config set <key> <value>
```

## Arguments

| Name | Required | Details | Default |
| --- | --- | --- | --- |
| `key` | Yes | Setting name, e.g. GOOGLE_CLIENT_ID | None |
| `value` | Yes | None | None |

## Options

This command has no command-specific options.
