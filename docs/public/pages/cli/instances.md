---
title: 'fluxmail instances'
description: 'Manage local and remote CLI instances'
updated: '2026-07-15'
---

<!-- This page is generated from the CLI command definitions. Run pnpm docs:generate to update it. -->

`fluxmail instances`

Manage local and remote CLI instances

## Usage

```bash
fluxmail instances [command]
```

## Options

This command has no command-specific options.

## Subcommands

| Command | Description |
| --- | --- |
| [`fluxmail instances list`](/docs/cli/instances-list) | List configured instances |
| [`fluxmail instances use`](/docs/cli/instances-use) | Select the default instance |
| [`fluxmail instances remove`](/docs/cli/instances-remove) | Remove a CLI profile and its local session |
